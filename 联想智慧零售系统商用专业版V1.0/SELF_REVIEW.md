# SELF_REVIEW

## 自查结果

- 独立目录已创建。
- V5 Enterprise 治理规范已放入 `docs/governance`。
- 已补充价格证据链、数据血缘、任务收口、审计留痕 4 个企业治理专项规则。
- 首页模块完整覆盖经营、SN、智店通、库存、报价、价保、提问、协作、验收。
- 已按原版零售系统菜单顺序重建 13 个主模块。
- 每个主模块已生成对应子菜单和真实 API 数据面板。
- 交互按钮均绑定实际视图切换或状态展示，不做假按钮。
- 数据模式已从静态样例切换为 `live_api_bridge`，不再把模块数据写成静态假数据。
- 数据文件已增加 `isSample`、`provenance`、`evidenceStatus`、`productionReadiness` 机器可读边界。
- 前端渲染已避免把业务数据直接拼入 `innerHTML`，改用 `textContent` 和 DOM 节点组装。
- 前端已增加 `system-state.json` schema 规范化和兜底样例，避免数据异常时静默空白。
- V6 AI Workstation 完整规则已同步至 `docs/governance/V6_AI_WORKSTATION`。

## 待生产接入

- 接入 SQL 数据库。
- 接入 FastAPI。
- 对每个子菜单做真实浏览器页面验收。
- 接入智店通真实导出和 SN 台账的证据链验收。
- 接入报价、营销、价保、竞品采集证据链验收。
- 增加真实门店员工试用验收。

## 子智能体审计闭环

- 治理审计指出：不能把静态样例包写成真实门店最终验收，已在验收报告中标记 `executed_not_closed`。
- 治理审计指出：V5 规则域不够细，已补价格证据链、数据血缘、任务收口、审计留痕规则。
- 数据审计指出：验收措辞容易误读，已把“已建立/基础通过”调整为“样例可用/本地样例运行正常”。
- UI 审计指出：业务数据 `innerHTML` 注入风险和半空白风险，已修复。
