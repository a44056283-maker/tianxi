任务名称：门店商品改价更新链路验证

任务目标：
你在 `商品 -> 门店商品` 页面，不是只做查看。
该页面已经通过真实页面操作确认具备操作、修改、确认、保存、导出权限。

本轮任务目标不是只看字段，而是把改价、下一步、保存、复核这整条链路讲清楚。

网页定位硬规则：

1. 这个页面是网页表单页，不是纯桌面软件。
2. 所以不允许把截图坐标点击当作主定位方式。
3. 必须优先使用：
   - 浏览器内可识别控件
   - 表单输入框
   - 步进器
   - 按钮
   - 文本 + 控件角色联合定位
4. 只有在控件完全不可识别时，坐标点击才允许作为辅助验证。
5. 不允许因为网页缩放、移位、滚动导致坐标漂移后，还继续把坐标盲点当成主要方案。

默认主入口：

1. 顶部单品检索
2. 命中目标商品
3. 进入编辑
4. 改价
5. 下一步
6. 保存
7. 回开复核

固定样本：

- 商品名称：`联想小新双接口固态闪存盘 XS730 活力橙（256GB）`
- 商品编码：`10006834`
- SKU编码：`20007533`
- PN/MTM：`QXB1T18370`
- 原价：`499`
- 目标价：`549`
- 计算规则：
  - `499 * 1.1 = 548.9`
  - 按尾数 `9` 收尾
  - 最终写入 `549`

执行顺序：
1. 进入 `商品 -> 门店商品`
2. 先识别搜索区字段：
   - 门店
   - 商品分类
   - 商品信息
   - PN/MTM
   - 69码
   - 商品类型
   - 商品状态
3. 确认当前页签是 `联想商品`
4. 识别 `搜索` 按钮
5. 优先走顶部单品检索，不先翻页扫表
6. 先明确每个检索字段的角色：
   - `商品信息`：主检索框，用于商品编码、名称、简称
   - `PN/MTM`：标准型号精确检索框
   - `69码`：条码精确检索框，只有知道真实 69 码时才用
   - `商品状态`：辅助过滤框，不单独做主检索
   - `商品分类`：辅助收窄范围，不单独做主检索
7. 先做检索练习一：
   - `商品信息 = 10006834`
   - 点击 `搜索`
8. 核对是否命中目标商品
9. 再做检索练习二：
   - `PN/MTM = QXB1T18370`
   - 点击 `搜索`
10. 核对是否仍命中同一商品
11. 再做检索练习三：
   - `商品信息 = 联想小新双接口固态闪存盘 XS730`
   - 点击 `搜索`
12. 核对是否仍命中同一商品
13. 对 `69码` 字段只做角色识别和点击定位，不假装完成命中，因为当前样本页显示 69 码为 `-`
14. 对 `商品状态` 只做辅助过滤识别，不单独拿它查单品
15. 对 `商品分类` 只做辅助收窄识别，不单独拿它查单品
16. 命中后进入编辑页
17. 在编辑页确认是否可见：
   - 建议零售价
   - 实际零售价
   - 售卖
   - 销售渠道
   - 是否显示库存信息
18. 判断 `实际零售价` 当前到底是：
   - 文本
   - 可输入控件
   - 或需要先进入下一步/后续页才能修改
19. 把 `实际零售价` 从 `499` 改成 `549`
20. 点击 `下一步`
21. 确认底部真实出现 `保存` 按钮
22. 点击 `保存`
23. 保存成功后回到列表页
24. 再次进入同一商品编辑页复核价格
25. 记录复核值

当前边界：
1. 页面能力已经真实确认可改、可保存、可导出
2. 但必须把遇到的问题写全，不能只报成功
3. 如果 `Computer Use` 对 `实际零售价` 步进器写值失败，必须明确记录：
   - 步进器在无障碍树里可见
   - 但直接写值失败
   - 失败信息或现象是什么
4. 如果纯键盘替换 `499.00¥` 不稳定，也必须记录
5. 如果视觉点击上下箭头命中率不稳定，也必须记录
6. 补救路径只允许：
   - 复用当前已登录 Chrome 会话
   - 在同一个真实页面里完成控件定位和改价保存
7. 禁止新开空白浏览器
8. 禁止切换到未登录 profile
9. 没有执行到的步骤，不能假装写成已完成
10. 顶部单品检索是默认主链路；如果没有先走检索就直接翻页找商品，必须在回执里说明原因
11. 不允许再写“截图点不中所以不会操作网页表单”这种结论

回执必须包含：
1. 搜索区字段是否识别完整
2. `联想商品` 页签是否命中成功
3. 搜索按钮是否识别成功
4. `商品信息 = 10006834` 是否命中成功
5. `PN/MTM = QXB1T18370` 是否命中成功
6. `商品信息 = 联想小新双接口固态闪存盘 XS730` 是否命中成功
7. 是否已明确说清 `69码` 只能在知道真实条码时使用
8. 是否已明确说清 `商品状态` 只是辅助过滤
9. 是否已明确说清 `商品分类` 只是辅助收窄
10. 目标商品是否定位成功
11. 编辑页是否进入成功
12. `实际零售价` 是否真实可见
13. `实际零售价` 当前是文本、输入控件，还是需要后续步骤
14. `下一步` 后是否真实出现 `保存`
15. 是否已真实执行改价
16. 是否已真实执行保存
17. 保存后是否返回列表页
18. 复核值是否已变为 `549.00¥`
19. 实际遇到的问题清单
20. 每个问题最后是怎么处理的
