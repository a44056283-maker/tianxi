# 联想智慧零售系统商业化进度追踪

- 更新时间：`2026-05-25 10:21:05 CST`
- 当前商业化成熟度：`49/100`
- 当前冲刺目标：`试点版 70/100`

## 1. 结论

- 当前系统已从“约 1/3 功能的自用系统”推进到 `商业化成熟度 49/100`。
- 现在可以继续按 `共创试点版` 方向推进，但还不能按成熟标准化 SaaS 对外规模售卖。
- 当前最值得先收口的三条主线仍是：`库存 / SN`、`出入库`、`营销 / 价保 / 补贴`。

## 2. 八维评分

| 维度 | 当前分值 | 满分 | 说明 |
| --- | ---: | ---: | --- |
| 产品完整度 | 11 | 20 | SKU、SN、库存流水主表已落 SQLite。；销售单、采购单基础表已存在。 |
| 数据架构 | 13 | 20 | 商业主库 SQLite 已长期运行。；snapshot_cache 已承接主要静态快照镜像。 |
| 商业交付能力 | 5 | 15 | 本地 API 已具备销售单/采购单基础写入入口。；本地零售同步模块和智店通同步骨架已存在。 |
| 多租户与权限 | 1 | 10 | 配置层已出现 tenantId 字段。 |
| API 与集成能力 | 6 | 10 | FastAPI 已有本地写入类接口输入模型。；本地同步和第三方同步骨架已存在。 |
| 稳定性与运维 | 6 | 10 | 定时任务体系、回执、watchdog 已成形。；备份归档脚本已落地。 |
| 文档与实施 | 5 | 10 | ai-context 文档、测试日志、交接文档较完整。；书签页 SQL 对接审计文档已单列。 |
| 定价与合同化 | 2 | 5 | 已形成按平台费 + 模块费 + 对接费 + 运维费的商业化定价思路。 |

## 3. 重点模块

| 模块 | 当前进度 | 说明 |
| --- | ---: | --- |
| 库存 / SN | 68% | 库存、SN、保修、主表和前端展示已经有真实主链，但仍存在前端过渡读链和商业化隔离缺口。 |
| 出入库 | 55% | 销售出库、采购入库、其他出库和订单闭环已经结构化，但当天金额快照缺口仍会导致任务未收口。 |
| 营销 / 价保 / 补贴 | 48% | 营销、价保、教育补已有展示与历史材料，但目前仍以快照镜像和局部历史表为主，离商业化历史链还差一段。 |

## 4. 当前阻塞

- 核心业务表尚未补 tenant_id / store_id，多租户和门店隔离还没有真正落库。
- 前端 `service.ts` 仍保留 mock 引用，说明展示层还有过渡实现没有完全清掉。
- 市场价格、链接、营销活动大量数据仍处于 `snapshot_cache` 过渡态，历史表尚未全面规范化。
- 当前 `sync_gap_queue` 仍有 30 条 open 缺口，出入库尚未完全商业级收口。
- 最近一次 watchdog 仍有 missed=1 / attention=1。

## 5. 定时任务状态摘要

- 任务总数：`12`
- 执行结果分布：`{"blocked_missing_input": 2, "real_completed": 7, "executed_not_closed": 3}`
- watchdog：`generatedAt=2026-05-25T01:01:01.175Z`，`missed=1`，`attention=1`，`pending=9`

## 6. 系统扫描依据

- SQLite 主库：`/Users/luxiangnan/Desktop/联想智慧零售项目/lenovo-smart-retail-cockpit/apps/api-server/data/retail-core.sqlite3`
- 核心表计数：`sku=3793`，`serial_item=393`，`inventory_movement=824`，`sales_order=71`，`order_sync_registry=48`，`sync_gap_queue=36`
- open 缺口：`30`
- 快照镜像文件数：`12`，默认书签口径：`api_strict=是`

## 7. 后续最该做

1. 先把 `库存 / SN / 出入库 / 营销` 相关核心业务表补 `tenant_id / store_id`，同时补 `tenant / store / user / role / permission` 基础表。
2. 继续把 `marketplace-prices / product-url-locks / marketing-boost / education-agent-scan / competitor-monitor / warranty-check-queue` 从 `snapshot_cache` 推进到正式 SQL 历史表或读模型。
3. 优先收口今天的出入库缺口：补齐缺失 `orderData / orderProductData` 金额快照，清空 `sync_gap_queue` 当前 open 项。
4. 把前端 `inventoryQuote/service.ts` 的 mock/静态兜底进一步收紧，确保商业化看板默认只吃 API/SQL 主链。
5. 补第一版对外产物：最小 API 文档、初始化导入 SOP、备份恢复 SOP、模块报价清单。

## 8. 用户/文档基线

- 原始商业化判断基线来自《商业化形成API对接.MD.docx》，其中把系统初始成熟度评为约 33/100。
- 用户要求以后在本对话持续推进商业化，并优先把库存、出入库、营销模块做到可售卖。
- 当前目标不是直接卖成熟 SaaS，而是先做到 70 分可卖试点版，再冲 85 分标准商用版，最后到 100 分连锁复制版。
