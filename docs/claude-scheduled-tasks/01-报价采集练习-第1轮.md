# Claude 任务：报价采集练习第 1 轮

执行日期：2026-05-17
项目根目录：`/Users/luxiangnan/Desktop/联想智慧零售项目/lenovo-smart-retail-cockpit`

## 任务目标

先练习，再形成定时任务。本轮只做报价采集练习，不直接写业务数据。

你需要练习两条报价链：

1. 分销群报价采集
2. 灰渠公众号报价采集

## 禁止事项

不要修改：

- `apps/web-cockpit/public/data/*.json`
- `apps/inventory-sync/artifacts/latest-*.json`
- `apps/api-server/data/*.sqlite3`

不要做：

- 不要伪造当天报价。
- 不要把旧文件重跑说成今日实时采集。
- 不要触发脚本、AppleScript、坐标点击、UI 自动化工具去点击微信或公众号。
- 不要自动点击 `Allow Once`；出现权限确认就停止并写阻塞。
- 不要后台抓微信数据。
- 不要绕过验证码、滑块、安全验证或登录风控。
- 不要读取、保存或输出密码、Token、Cookie、验证码、登录态。
- 不要直接改主仓库快照。

## 微信操作规则

微信和公众号只允许人工手动点击。

Claude 只允许：

- 告诉用户下一步需要手动点哪里。
- 等用户手动操作完成后，再继续判断结果。
- 读取用户手动下载或保存到本机的文件、截图、OCR 文本。

Claude 禁止：

- 运行 `scripts/capture_wechat_article_pages.sh`、`scripts/capture_and_ocr_wechat_quote.sh` 或任何会自动点击微信的脚本。
- 使用 AppleScript、坐标点击、UI 自动化、Computer Use、浏览器自动化去操作微信。
- 自动点击 `Allow Once` 或其它权限确认按钮。
- 在微信界面状态不清楚时继续推进。

## Allow Once 处理

如果出现 `Allow Once`：

- 不要点击。
- 状态写 `blocked_permission_prompt`。
- 说明这是权限确认，当前路径不能无人值守自动化。
- 改为等待用户手动保存文件或截图后，再读取文件继续判断。

## 先读规则

开始前先读取：

- `AGENTS.md`
- `docs/ai-context/13_SCHEDULED_TASK_SOPS.md`
- `docs/ai-context/04_NEXT_ACTIONS.md`
- `docs/ai-context/09_CODEX_HANDOFF.md`

重点查找这些关键词：

- `分销商群报价`
- `灰渠公众号报价`
- `微信`
- `blocked_missing_input`
- `blocked_page_or_login`
- `blocked_permission_prompt`
- `carry_forward`

## 分销群报价练习

目标：判断 2026-05-17 是否有真实分销群报价证据。

优先检查：

- `apps/inventory-sync/artifacts/manual/wechat-quote-collection/current/`
- `/Users/luxiangnan/Downloads/codex-installs/wechat-selkies/config/xwechat_files/`
- `/Users/luxiangnan/Downloads`
- `apps/inventory-sync/artifacts/`
- `apps/web-cockpit/public/data/latest-distributor-quotes.json`
- `docs/ai-context/09_CODEX_HANDOFF.md`

如果找到当天文件或截图：

- 记录完整路径。
- 记录文件名、修改时间、是否为 2026-05-17。
- 不写入主业务快照。
- 状态可写 `real_completed` 或 `executed_not_closed`，但必须说明原因。

如果没有找到：

- 不要伪造。
- 如果微信不可用、未登录或白屏，写 `blocked_page_or_login`。
- 如果只是没有当天输入，写 `blocked_missing_input`。

## 灰渠公众号报价练习

目标：判断 2026-05-17 是否有真实灰渠公众号原文、截图或 OCR 文本。

优先检查：

- `apps/inventory-sync/artifacts/`
- `apps/web-cockpit/public/data/latest-gray-wholesale-quotes.json`
- `apps/web-cockpit/public/data/latest-wechat-quote-evidence.json`
- `apps/web-cockpit/public/data/latest-quote-ocr-queue.json`
- `docs/ai-context/04_NEXT_ACTIONS.md`
- `docs/ai-context/09_CODEX_HANDOFF.md`

如果只有旧日期有效报价：

- 写 `carry_forward_candidate`。
- 明确说明不是当天新采。

如果需要进入微信或公众号：

- 先识别当前界面状态，然后请用户手动点击。
- 遇到登录、白屏、验证码、滑块、安全验证、文章打不开、正文掩码价，立即停止该路径。

## 输出文件

请把回执写到项目目录：

- `docs/claude-scheduled-tasks/receipts/2026-05-17-报价采集练习-第1轮-回执.md`
- `docs/claude-scheduled-tasks/receipts/2026-05-17-报价采集练习-第1轮-回执.json`

如果 `receipts/` 不存在，可以创建。

## 回执格式

```text
结论：
- 分销群报价：real_completed / executed_not_closed / blocked_missing_input / blocked_page_or_login
- 灰渠公众号报价：real_completed / executed_not_closed / blocked_missing_input / blocked_page_or_login / carry_forward_candidate

证据：
- 路径：
- 文件名：
- 文件日期：
- 是否为 2026-05-17：

实际操作：
- 检查了哪些路径：
- 是否打开微信或公众号：
- 是否所有微信点击均由用户手动完成：
- 当前页面状态：

未做事项：
- 未修改 public/data：
- 未修改 latest-*.json：
- 未修改 SQLite：

下一轮练习建议：
- 1.
- 2.
- 3.
```

## 成功标准

本轮成功不是一定采到价格，而是你能真实区分：

- 当天真实报价
- 旧报价沿用
- 缺原始输入
- 页面或登录阻塞
- 已执行但未收口
